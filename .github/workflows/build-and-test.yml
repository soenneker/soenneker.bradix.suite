name: build-and-test

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  PipelineEnvironment: true
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore tests
        run: |
          dotnet restore test/Soenneker.Bradix.Suite.Tests/Soenneker.Bradix.Suite.Tests.csproj --verbosity minimal
          dotnet restore test/Soenneker.Bradix.Suite.Playwright.Tests/Soenneker.Bradix.Suite.Playwright.Tests.csproj --verbosity minimal

      - name: Build tests
        run: |
          dotnet build test/Soenneker.Bradix.Suite.Tests/Soenneker.Bradix.Suite.Tests.csproj --configuration Release --no-restore --verbosity minimal
          dotnet build test/Soenneker.Bradix.Suite.Playwright.Tests/Soenneker.Bradix.Suite.Playwright.Tests.csproj --configuration Release --no-restore --verbosity minimal

      - name: Test
        run: |
          dotnet test --project test/Soenneker.Bradix.Suite.Tests/Soenneker.Bradix.Suite.Tests.csproj --configuration Release --no-build --no-restore --verbosity minimal
          dotnet test --project test/Soenneker.Bradix.Suite.Playwright.Tests/Soenneker.Bradix.Suite.Playwright.Tests.csproj --configuration Release --no-build --no-restore --verbosity minimal

      - name: Pack
        run: dotnet pack src/Soenneker.Bradix.Suite/Soenneker.Bradix.Suite.csproj --configuration Release --no-build --no-restore --output . --verbosity minimal
