@namespace Soenneker.Bradix

@inherits BradixComponent

@implements IBradixFormSubmit
<button @attributes="SubmitAttributes">
    @ChildContent
</button>

@code {
    private Dictionary<string, object> SubmitAttributes => BuildAttributes(
        ("type", "submit"));
}
